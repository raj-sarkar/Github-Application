/**
 * Props for header component
 * @property isDesktop - boolean value of view width is desktop or not
 * @property showLoginButton - true if login button has to be shown
 */
export type HeaderProps = {
    isDesktop: boolean;
    showLoginButton: boolean;
};
