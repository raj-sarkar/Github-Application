/**
 * Props for repositories table container
 * @property isDesktop - true if view width is desktop
 * @property username - username of user
 */
export type RepoTableProps = {
    isDesktop: boolean;
    username: string;
};
